{
  "name": "@ops-pilot/savings-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@ops-pilot/config": "workspace:*",
    "@ops-pilot/observability": "workspace:*",
    "pg": "8.13.3"
  },
  "devDependencies": {
    "@types/pg": "8.11.11",
    "tsx": "4.19.3"
  }
}
