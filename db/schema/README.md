# Database Schema

Core entities:

- `tenants`, `users`
- `connectors`
- `opportunities`
- `actions`
- `policies`
- `savings_ledger`
- `cloud_events`

Use `db/migrations/0001_init.sql` to initialize schema and `db/seeds/0001_seed.sql` to load demo data.
